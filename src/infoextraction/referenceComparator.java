package infoextraction;

/**
 * Created by Gabriela on 06-Jul-17.
 */


//TODO: eventually measure the edit distance and choose what to do when only one index differs
// TODO: consider assigning a confidence score instead of a bool
public class referenceComparator {
//
//    public boolean compare (referenceRecorder a, referenceRecorder b){
//        if (a.getReference().equals(b.getReference())){
//            if(a.getStartIndex() == b.getEndIndex()){
//                if (a.getEndIndex() == b.getEndIndex()){
//                    return true;
//                }
//            }
//        }
//        return false;
//    }
}
